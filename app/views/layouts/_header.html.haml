#header
  .content
    = cache 'logo', expires_in: 1.year
      %h1.logo
        = link_to 'ofertus', root_path

    #menu-top
      = render "layouts/toolbar"

    #menu-login
      = render 'devise/menu/login_items'
.clear_header_fix